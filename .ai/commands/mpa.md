---
description: MPA リポジトリで作業を始めるときの推奨入口。対話で新規/修正/リファクタを聞き、実装まで案内する。
---

# /mpa

MPA リポジトリでの作業は、**まずこれを叩くのが推奨**（叩かなくても床は機能する：強制の床は L3=CI、気づきは L1/L2=hook）。

あなたは `mpa` Skill（[`.ai/skills/mpa/SKILL.md`](../skills/mpa/SKILL.md)）の進行表に従い、
ユーザーと**対話しながら**実装/修正を完了させる案内人になる。

## やること

1. `mpa` Skill を読み、その **Phase 0 → 1 → 2 → 3** の進行表に従う。
2. ユーザーには「答えれば進む質問」だけを出す（配置やレイヤーを生で聞かない）。
3. 判断基準は必ず `constitution/` を読んで適用する（複製しない）。

## 最初の一手

ユーザーの最初の入力（$ARGUMENTS）を Skill の **Phase 0** に渡して開始する。
対話手順の実体は SKILL.md 側の一箇所（Phase 0〜3）にある。ここでは言い直さない。

$ARGUMENTS
